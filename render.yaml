# Render deployment configuration for DNSWorth backend
# This file tells Render how to deploy the backend service

services:
  - type: web
    name: dnsworth-backend
    env: node
    plan: free
    region: oregon
    buildCommand: |
      echo "Cleaning and installing backend dependencies..."
      cd backend
      rm -rf node_modules package-lock.json
      npm install --force
      echo "Backend dependencies installed successfully!"
    startCommand: |
      echo "Starting backend server..."
      cd backend
      npm start
    rootDir: backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /health
    autoDeploy: true
    buildFilter:
      paths:
        - backend/**
    routes:
      - type: rewrite
        source: /*
        destination: /backend/*
